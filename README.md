# Rust-Project-Linkchecker
program that takes a text file as input containing a list of URLs in markdown format for LambdaClass Engineering Residency

```
  make build  - Build the release binary
  make run    - Run the project
  make test   - run tests
  make clean  - Remove build artifacts
```
